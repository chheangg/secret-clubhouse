<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('head', { page_title: 'Secret clubhouse' }) %>
</head>
<body>
  <div>
    <% if (locals.currentUser) {%>
      <% console.log(locals) %>
      <h1>Membership</h1>
      <% if (currentUser.isMember) { %>
        <p>Hey there <%= currentUser.full_name %>, You are already a member of this club!</p>
      <% } else { %>
        <p>You are not a member of this club!</p>
        <form method="POST" action="">
          <label for="secret-passcode">Secret passcode</label>
          <br>
          <input id="secret-passcode" type="password" name="secret_passcode" placeholder="no hint haha">
          <input value=<%= currentUser._id.toString() %> name="userId" type="hidden">
          <button type="submit">Enter</button>
          <% if (locals.error) { %> 
            <p><%= error %></p>
          <% } %>
        </form>
      <% } %>
    <% } else { %>
      <h1>You must be logged in to see this</h1>
    <% } %>
  </div>
</body>
</html>